<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="/stylesheets/style.css">
</head>

<body>
    <div class="main w-full min-h-screen bg-zinc-900 relative">
        <div class="form-section p-10">
            <h1 class="text-white text-3xl pb-6 mb-4 border-b border-white block">Editing Mode</h1>
            <form method="post" action="/edit">
                <input class="block mt-3 px-5 py-3 bg-zinc-600 text-white w-full rounded-lg" type="text" name="heading"
                    placeholder="Title goes here" value="<%= heading.split('_').join(' ').split('.txt').join('') %>">
                <input hidden type="text" name="currentAddress" value="<%= heading %>">
                <textarea class="block mt-3 px-5 py-3 extended-none bg-zinc-600 text-white w-full rounded-lg"
                    placeholder="Description" name="dets"><%= data %></textarea>
                <div class="actions flex items-center justify-start gap-2">
                    <input class="mt-3 px-5 py-3 bg-blue-500 text-white rounded-lg" type="submit" value="Edit Task">
                    <a class="mt-3 px-5 py-3 bg-red-500 text-white rounded-lg" href="/">Cancel Editing</a>
                </div>
            </form>
        </div>
        
        
    </div>
    <script src="/javascripts/script.js"></script>
</body>

</html>